# Baseline B01 · Python as research infrastructure, as the posting uses it

**Learner:** Krystal Martinez · **Date taken:** ____________ · **Form:** A (first administration) · **v02**
**Specimen:** `specimens/audit_against_ssots.py` (open it; every item that names a function is
pointing at that file) · **Rules:** prose answers, no code written, no AI assistance, skip freely
(a skip is recorded as 0 with a note).

**Part 1 is the content. Part 2 is your self-assessment and comes only after Part 1 is finished.**
Nothing in Part 1 asks how sure you are; answer the question and move on.

Each item names the span of the posting it is scoped to, so nothing here is Python-in-general.

---

## Band A · reading Python

**A1 · modules and imports** — *"Strong Python programming skills"*
The file begins with `from urllib import request, error` and `import json`. What is being brought in,
where does it come from (does anyone have to install it?), and what would change for a researcher
installing this tool if it used the third-party `requests` library instead?
> answer:

**A2 · entry point and arguments** — *"workflows researchers use to implement and iterate"*
The last two lines are `if __name__ == "__main__":` and `sys.exit(main())`. What does that first
line decide? And in `main()`, the `--ssot` flag is declared with `action="append"`: what does that
let a researcher do at the command line that a plain flag would not?
> answer:

**A3 · functions and return values** — *"Strong Python programming skills"*
`part_3_what(...)` ends with `return system, contents`, and `main()` receives it as
`system, contents = part_3_what(...)`. What kind of thing is being returned, how is it being taken
apart, and what would break if the function returned only `contents`?
> answer:

**A4 · nested data structures** — *"implement the scoring"*
In `part_5_then`, the model's text is found by:
`"".join(part.get("text", "") for c in resp.get("candidates", []) for part in c.get("content", {}).get("parts", []))`
Describe, in plain words, the shape of `resp` this line assumes (which pieces are lists, which are
dictionaries), and why it uses `.get(..., [])` rather than square brackets.
> answer:

**A5 · loops and early exit** — *"better retries"*
In `send`, a `for i in range(tries)` loop contains `continue` in some branches, `raise` in others,
and a `return` on success. Walk the loop: when does it go around again, when does it stop early, and
what does the final `raise RuntimeError("unreachable")` after the loop protect against?
> answer:

**A6 · files, paths and encodings** — *"build the dataset"*
`file_part` has two branches. What decides which branch a file takes, why is an image turned into
base64 text before it goes into the request body, and what is `encoding="utf-8", errors="replace"`
guarding against on the text branch?
> answer:

**A7 · reading a traceback** — *"determine whether it's the model, the harness, the data, or the infrastructure"*
A run ends with a traceback whose last line is
`json.decoder.JSONDecodeError: Unterminated string starting at: line 212 column 21`
raised from inside `part_5_then`. Name one file that is already on disk at that moment, say why it is
there, and say what you would open first to tell whether the model, the harness, or a setting caused
it.
> answer:

## Band B · infrastructure patterns

**B1 · configuration and secrets** — *"production or research infrastructure"*
`part_2_who` reads the key with `os.environ.get(key_var)`. Where does that value come from at run
time, what would be wrong with writing `KEY = "…"` in the file, and what does the `--key-var` flag
let you change without editing code?
> answer:

**B2 · exceptions as routing** — *"take a flaky distributed eval pipeline and make it boring"*
The file defines `class Quota(Exception)`. `send` raises it on a 429 whose body says quota, and
`main()` catches it to step down the model ladder. Why a custom exception class rather than returning
`None` or an error string? And, by contrast, what path does an HTTP 500 take through `send`?
> answer:

**B3 · retries and backoff** — *"better retries"*
`wait = min(30, 2 ** i * 5)`. Give the waits for `i = 0, 1, 2`. Why cap the wait? And name, or
describe in your own words, the property a request must have for retrying it to be safe at all.
> answer:

**B4 · bank before parse** — *"run reliably"*
`part_5_then` writes `raw_response.json` and `request_manifest.json` **before** calling
`json.loads(text)`. What failure does that ordering protect against, and what would be lost if the
two steps were reversed?
> answer:

**B5 · observability from inside** — *"better observability"*, *"observability, monitoring, or experiment-tracking systems"*
List what this program records about a run (the manifest fields count). Then name one thing you
would add so that a researcher could tell, a week later, why two runs on the same artifact disagreed.
> answer:

**B6 · sequential versus concurrent** — *"hundreds of evals run reliably"*
This program makes one call. A sibling harness on 2026-09-19 first ran **four workers in parallel**
against a provider quota of **20 requests per model per day** and got zero successes; a later version
ran **one call at a time**, paced, stepping down to the next model when a quota hit, and finished.
In your own words: what did concurrency buy, what did it cost here, and what single fact about the
provider decided which design was right?
> answer:

**B7 · process lifecycle** — *"reliable at scale"*, *"on-call ... when training runs are live"*
The same day, a run launched with `nohup python3 … &` was "stopped" by killing the process id the
shell reported, yet it kept running for another minute. What was killed and what kept running? How
would you find the real process, and why does the shell's reported id mislead here?
> answer:

**B8 · package and CLI shape** — *"the tooling, libraries, and workflows researchers use"*
Today this is a script in a folder. Name two things that would have to exist for a researcher to
`pip install` it and run it as a command (say, `audit-ssots …`) from any directory. Why does that
matter more for "workflows researchers use to iterate" than the script's correctness does?
> answer:

**B9 · reproducibility knobs** — *"how prompting, sampling, and scaffolding choices affect results"*, *"validate against known signals"*
`part_4_how` sets `temperature` to 0; the manifest records the model id and a sha256 of every input
file. For each of those three, say which of the posting's "prompting, sampling, and scaffolding
choices" it pins down. Then name one thing that could still differ between two runs with identical
manifests.
> answer:

---

## Part 2 · Self-assessment (do this only after all sixteen answers are in, and before any score is shown)

This is a separate item set on purpose: judging yourself while answering changes the answering.
For each concept, circle the one description that fits what you can do today. These are
descriptions of behaviour, not grades, and none of them is wrong.

| Level | What it means |
|---|---|
| **1** | I could not follow this part of the program even with the file open. |
| **2** | I could follow it with the file open, but I could not direct a change to it. |
| **3** | I could direct a change to it and say what would happen. |
| **4** | I could diagnose a failure in it from its records alone and direct the fix. |

| Concept | 1 | 2 | 3 | 4 |
|---|---|---|---|---|
| A1 modules and imports | | | | |
| A2 entry point and arguments | | | | |
| A3 functions and return values | | | | |
| A4 nested data structures | | | | |
| A5 loops and early exit | | | | |
| A6 files, paths and encodings | | | | |
| A7 reading a traceback | | | | |
| B1 configuration and secrets | | | | |
| B2 exceptions as routing | | | | |
| B3 retries and backoff | | | | |
| B4 bank before parse | | | | |
| B5 observability from inside | | | | |
| B6 sequential versus concurrent | | | | |
| B7 process lifecycle | | | | |
| B8 package and CLI shape | | | | |
| B9 reproducibility knobs | | | | |

**Looking back at Part 1 as a whole:** which band felt like reading, and which felt like guessing?
> ______________________________________________________________________________

## Journal (optional, always yours, never edited by anyone else)

One line: what surprised you, what you want next, or what you would tell yourself from a week ago.
> ______________________________________________________________________________

*Form A, v02, 2026-09-20. v01 (2026-09-19) is retired to `deprecated/`; it asked for a confidence
rating beside every content item, which v02 removes per the learner's ruling that self-scoring must
be a distinct item set from the content under evaluation. Scored against a key held outside this
repo. The result is a 16-bar profile with two band totals (A of 14, B of 18), a per-concept
calibration reading from Part 2, and the journal line verbatim; no single number.*
