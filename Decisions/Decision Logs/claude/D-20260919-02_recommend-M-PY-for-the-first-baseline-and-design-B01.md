## D-20260919-02 — Recommend M-PY (Python as research infrastructure) for the first baseline, and design B01

**Action:** Compile the modular learn inventory from the posting and her highlighted copy (ten
modules, verbs proposed not ratified); recommend M-PY for tonight's single baseline; design and write
Baseline B01 Form A (16 concepts in two bands, a 0/1/2 profile, confidence before every answer) with
its key held in the private workbench.

**Method:** Authored by this seat. Scope for every item is a quoted span of the posting. Specimen is
the course's own program rather than her census harness, because the harness lives in a private
workbench and publishing it is her call. Two concepts without a specimen (concurrency, process
lifecycle) are posed from the day's real episodes.

**Repetition:** 1 (D-01 shares the Method of this seat authoring course apparatus). New rationale:
D-01 was repository set-up; this is instrument design, with its own alternatives.

**Alternatives rejected (module for the first baseline):**
- **M-DIST, distributed eval execution.** Second choice. Rejected for tonight: its posting scope
  (hundreds of evals against checkpoints during a live RL run) is hard to baseline conceptually
  without the Python vocabulary underneath it, and half its items would measure transfer from the
  human-systems version she says she already has, which is a different construct.
- **M-DASH, model-health dashboards.** Rejected: her data-visualization span is yellow; the gap is the
  tool and the during-training context, both of which need the learning experience's tool choice
  first.
- **M-TRN, ML training infrastructure.** Rejected: a baseline on a module with no prior exposure
  floors out and decides nothing about sequencing.
- **M-DIAG, model change versus infrastructure issue.** Rejected: a reasoning skill that sits on top
  of M-PY and M-DIST; baseline it once those have moved.

**Alternatives rejected (instrument design):**
- **A single score.** Rejected: her purple carries two needs (refresh basics, learn patterns), and a
  single number hides the band split the baseline exists to find.
- **Items at remember level only.** Rejected: the posting's verbs are build, harden, improve, run;
  the items are posed at understand, apply and analyze on a real program, in her modality (reading
  and directing, no code written).
- **Key in the public repo beside the instrument.** Rejected: a re-administered baseline needs its
  key out of the learner's path.

**Rationale:** M-PY is the only minimum qualification she marked as a gap and the material every
orange module is made of; it is baseline-able tonight from a phone; and the two-band profile gives the
learning-experience design session its first hard input, the sequence of the Python strand.

**Falsifier:** the profile comes back flat (all 2s or all 0s) with no band split, in which case the
instrument's difficulty is mis-set and Form A is replaced before Form B is written; or she rules a
different module first, in which case B01 stands as M-PY's baseline and the new module gets B02.

Recorded after the writing by Flaudechamba A.-L. Formative Horizon v01 (Claude Fable 5.1), session
20cd8283, 2026-09-19.
